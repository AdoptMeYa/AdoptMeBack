logging:
    level:
        org:
            hibernate:
                SQL: DEBUG
                type: TRACE
server:
    port: 8080
spring:
    datasource:
        password: mysql
        url: jdbc:mysql://localhost:3306/demo
        username: root
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL5InnoDBDialect
